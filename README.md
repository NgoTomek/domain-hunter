# gems-feed

Auto-updated **data branch** for domain-hunter. GitHub Actions writes the gem ledger here so `main` stays clean:

- **`GEMS.md`** — human-readable leaderboard of available finds
- **`data/gems.json`** — the full ledger

Code + docs live on `main`.
